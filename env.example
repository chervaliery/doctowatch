# Copy to .env and fill in your Mailjet credentials.
# Restrict permissions: chmod 600 .env
# Do not commit .env (it is in .gitignore).

export MJ_APIKEY_PUBLIC="your-mailjet-api-key"
export MJ_APIKEY_PRIVATE="your-mailjet-api-secret"
